[package]
name = "dwmpi"
version = "0.1.0"
edition = "2021"

[features]
default = []
# Backward-compatible no-op feature; rsmpi is now always used.
rsmpi_backend = []

[dependencies]
types = { path = "../types" }
mpi = { version = "0.8.1", default-features = false, features = ["complex"] }
